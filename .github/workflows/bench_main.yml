name: Benchmarks

on:
  push:
    branches: [ "checksum_bench" ]

permissions:
  id-token: write
  contents: write

jobs:
  integration:
    name: Benchmarks
    uses: ./.github/workflows/bench.yml
    with:
      ref: ${{ github.event.after }}
